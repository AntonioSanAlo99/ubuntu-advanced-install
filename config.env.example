# ============================================================================
# CONFIGURACIÓN UBUNTU ADVANCED INSTALL
# ============================================================================
# Este archivo contiene todas las variables de configuración.
# Copia este archivo a config.env y ajusta los valores.
#
# cp config.env.example config.env
# nano config.env
#
# Luego ejecuta: sudo ./install.sh --auto

# === SISTEMA BASE ===
UBUNTU_VERSION="noble"
HOSTNAME="ubuntu"
USERNAME="user"
USER_PASSWORD="changeme"
ROOT_PASSWORD="changeme"

# === LOCALIZACIÓN ===
TIMEZONE="Europe/Madrid"
LOCALE="es_ES.UTF-8"
KEYBOARD_LAYOUT="es"

# === HARDWARE ===
IS_LAPTOP="false"          # true para laptops
HAS_WIFI="true"            # true para instalar WiFi
HAS_BLUETOOTH="true"       # true para instalar Bluetooth

# === DISCO ===
TARGET_DISK="/dev/sda"
DUAL_BOOT="false"
UBUNTU_SIZE_GB="100"

# === COMPONENTES ===
INSTALL_GNOME="true"

# GNOME - Personalización
GDM_AUTOLOGIN="false"
GNOME_OPTIMIZE_MEMORY="false"
GNOME_TRANSPARENT_THEME="false"  # Recomendado: false - Adwaita por defecto es excelente

INSTALL_MULTIMEDIA="true"
INSTALL_DEVELOPMENT="false"
INSTALL_GAMING="false"

# === OPTIMIZACIONES ===
MINIMIZE_SYSTEMD="false"
ENABLE_SECURITY="false"
USE_NO_INSTALL_RECOMMENDS="false"

# === LAPTOP (si IS_LAPTOP="true") ===
# POWER_MANAGER="1"  # 1=power-profiles-daemon (recomendado), 2=TLP

# === DESARROLLO (si INSTALL_DEVELOPMENT="true") ===
# Opciones interactivas durante instalación:
# - Visual Studio Code (s/n)
# - NodeJS (1=No, 2=Ubuntu repos, 3=NodeSource)
# - Rust con rustup (s/n)

# === GAMING (si INSTALL_GAMING="true") ===
# Se instalan automáticamente:
# - Steam, Faugus Launcher, Heroic
# - ProtonUp-Qt, umu-launcher
# - Proton-Cachyos
# - GameMode + MangoHud
